spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/rag_chatbot}
    username: ${DB_USERNAME:rag_user}
    password: ${DB_PASSWORD:ragrag123}
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
